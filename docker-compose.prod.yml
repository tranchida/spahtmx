services:
  app:
    build: .
    env_file: .env
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      SEED_DB: ${SEED_DB:-false}
      PORT: "8080"
      